import 'package:flutter/material.dart';

class RunbooksScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Runbooks')),
      body: Center(child: Text('Runbooks list will show here')),
    );
  }
}
